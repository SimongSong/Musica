## How to run this?
- if you just want to run react app 
`npm run start`
- if you want to run electron app 
`npm run electron`

So ideally in one terminal you run `npm run start`, then open another terminal and run `npm run electron`
